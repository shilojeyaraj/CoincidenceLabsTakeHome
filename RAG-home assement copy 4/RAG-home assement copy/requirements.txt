# Core dependencies for the Multi-Document RAG assessment
# Install with: pip install -r requirements.txt

# Embeddings (pick one)
sentence-transformers>=2.2.0    # Free, local — recommended for getting started
# openai>=1.0.0                 # Alternative: use text-embedding-3-small

# Vector search (pick one)
faiss-cpu>=1.7.0                # Recommended — simple and fast
# chromadb>=0.4.0               # Alternative: includes persistence

# LLM for generation and conflict detection (pick one)
openai>=1.0.0                   # GPT-4o-mini is cost-effective
# anthropic>=0.20.0             # Alternative: Claude Haiku

# Utilities
numpy>=1.24.0
tqdm>=4.60.0
